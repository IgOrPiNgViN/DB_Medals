# Скопируйте в .env (docker compose + клиент + импорт данных).
POSTGRES_PASSWORD=changeme
SERVER_URL=http://localhost:8000
# PostgreSQL в Docker доступен с хоста на 5433 (внутри сети compose — db:5432)
DATABASE_URL=postgresql://postgres:changeme@localhost:5433/awards_db
