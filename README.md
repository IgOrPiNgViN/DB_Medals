# ООН ПКР — База данных «Отраслевые общественные награды промышленного комплекса России»

Клиент-серверное приложение для управления базой данных наград, лауреатов и наградного комитета.

## Архитектура

- **Сервер**: FastAPI + SQLAlchemy + PostgreSQL
- **Клиент**: PyQt5 (десктопное приложение)
- **Связь**: REST API через HTTP

## Требования

- Python 3.10+
- PostgreSQL 15+

## Установка и запуск

### 1. Установка PostgreSQL

Скачайте и установите PostgreSQL: https://www.postgresql.org/download/windows/

Создайте базу данных:
```sql
CREATE DATABASE awards_db;
```

### 2. Запуск сервера (API, без окна)

Это **только бэкенд** — в браузере откроется только документация, если вы её откроете сами. Окно программы не появится.

```bash
cd server
pip install -r requirements.txt

# Настройте подключение к БД (по умолчанию: postgresql://postgres:1234@localhost:5432/awards_db)
# Можно задать переменную окружения:
# set DATABASE_URL=postgresql://user:password@host:port/awards_db

python main.py
```

Сервер: http://localhost:8000 — Swagger: http://localhost:8000/docs

### 3. Запуск клиента (окно приложения PyQt5)

**Откройте второй терминал** (сервер из шага 2 должен продолжать работать).

```bash
cd client
pip install -r requirements.txt

# Если сервер на другом адресе:
# set SERVER_URL=http://192.168.1.100:8000

python main.py
```

Именно эта команда открывает **десктопное приложение** с боковым меню.

**Ошибка «Could not find the Qt platform plugin windows»**  
В `client/main.py` при старте выставляются пути к плагинам PyQt5. Если ошибка остаётся, в PowerShell укажите каталог вручную (путь подставьте свой, где установлен Python):

```powershell
$py = python -c "import PyQt5, os; print(os.path.dirname(PyQt5.__file__))"
$env:QT_PLUGIN_PATH = "$py\Qt5\plugins"
$env:QT_QPA_PLATFORM_PLUGIN_PATH = "$py\Qt5\plugins\platforms"
python main.py
```

Либо переустановите PyQt5: `pip uninstall PyQt5 PyQt5-Qt5 PyQt5-sip -y` и `pip install PyQt5`.

## Структура проекта

```
├── server/                          # FastAPI бэкенд
│   ├── main.py                      # Точка входа сервера
│   ├── config.py                    # Настройки (DATABASE_URL, порт)
│   ├── database.py                  # SQLAlchemy engine и session
│   ├── models/                      # SQLAlchemy модели (20 таблиц)
│   │   ├── award.py                 # Награды, характеристики, производство, склад
│   │   ├── laureate.py              # Лауреаты, привязка к наградам, ЖЦ
│   │   ├── committee.py             # Наградной комитет, права подписи
│   │   └── voting.py                # Бюллетени, голоса, протоколы, выписки
│   ├── schemas/                     # Pydantic-схемы для API
│   ├── routers/                     # FastAPI роутеры (60+ эндпоинтов)
│   │   ├── awards.py                # /api/awards/...
│   │   ├── laureates.py             # /api/laureates/...
│   │   ├── committee.py             # /api/committee/...
│   │   ├── voting.py                # /api/voting/...
│   │   ├── reports.py               # /api/reports/...
│   │   └── backup.py                # /api/backup/... (выгрузка/восстановление БД)
│   └── requirements.txt
│
├── client/                          # PyQt5 клиент
│   ├── main.py                      # Точка входа клиента
│   ├── config.py                    # URL сервера
│   ├── api_client.py                # HTTP-клиент (60+ методов)
│   ├── ui/
│   │   ├── main_window.py           # Главное окно с навигацией
│   │   ├── awards/                  # Раздел «Награды»
│   │   │   ├── awards_cards.py      # Карточки наград
│   │   │   ├── award_detail.py      # Детали (характеристика, учреждение, разработка...)
│   │   │   ├── lifecycle.py         # Жизненный цикл наград
│   │   │   ├── warehouse.py         # Склад
│   │   │   └── current_awards_report.py  # Отчёт «Актуальные награды»
│   │   ├── laureates/               # Раздел «Лауреаты»
│   │   │   ├── laureate_cards.py    # Карточки лауреатов
│   │   │   ├── laureate_detail.py   # Детальная карточка
│   │   │   ├── laureate_lc.py       # ЖЦ лауреата (6 этапов)
│   │   │   ├── awards_laureates.py  # Отчёт «Награды-лауреаты»
│   │   │   ├── incomplete_lc.py     # Незавершённый ЖЦ
│   │   │   ├── lc_stages_report.py # Отчёт по этапам ЖЦ (ТЗ)
│   │   │   └── statistics.py        # Статистика
│   │   ├── committee/               # Раздел «Наградной комитет»
│   │   │   ├── committee_list.py    # Список НК
│   │   │   └── member_card.py       # Карточка члена НК
│   │   ├── voting/                  # Раздел «Голосование»
│   │   │   ├── bulletin.py          # Создание бюллетеня
│   │   │   ├── monitoring.py        # Мониторинг ответов
│   │   │   ├── vote_counting.py     # Подсчёт голосов (порог 65%)
│   │   │   ├── protocol.py          # Протоколы
│   │   │   ├── extract.py           # Выписки из протокола
│   │   │   └── ppz_submission.py    # Представления ППЗ
│   │   └── service/
│   │       ├── db_export.py         # Выгрузка/восстановление БД
│   │       └── access_tables_page.py  # Просмотр зеркала таблиц Access
│   ├── resources/
│   │   └── styles.qss               # Стили интерфейса
│   └── requirements.txt
│
├── scripts/
│   └── verify_project.py            # Проверка: compileall + импорт API
└── migration/
    ├── inspect_access.py            # Анализ структуры Access-базы
    └── access_detailed.json         # Извлечённая схема Access
```

## Разделы приложения

### Награды
- Карточки наград (создание, удаление, фильтрация по типу)
- Детали: характеристика, учреждение, разработка, согласование, производство
- Жизненный цикл наград (сводный отчёт по 4 типам наград)
- Склад (физический учёт с оповещениями при остатке < 10 шт.)

### Лауреаты
- Карточки лауреатов с категориями (сотрудники, ветераны, университеты, НИИ, орг.)
- Привязка лауреата к награде
- ЖЦ лауреата (6 этапов: выдвижение → голосование → решение → оформление → вручение → опубликование)
- Отчёты: награды-лауреаты, незавершённый ЖЦ, **этапы ЖЦ** (сколько на каждом этапе), статистика

### Наградной комитет
- Список членов НК (действующие/не действующие)
- Карточки с правами подписи и полномочиями
- Назначение подписантом удостоверений / уполномоченным по награде

### Голосование
- Создание бюллетеней (разделы, вопросы, рассылка)
- Мониторинг ответов (цветовая индикация: зелёный/красный/серый)
- Подсчёт голосов (порог принятия решения: 65%)
- Формирование протоколов
- Выписки из протоколов
- Представления на награждение ППЗ

### Сервис
- Полный бэкап PostgreSQL (pg_dump)
- Восстановление из дампа
- Экспорт отдельных таблиц в CSV

## Миграция данных из Access

### Почему в приложении «пусто»

Новая БД **PostgreSQL** изначально **без строк**: склад, лауреаты и т.д. заполняются после **импорта** или **ввода вручную**. Это не баг интерфейса.

Присланные файлы `.accdb` — **часто только оболочка**: таблицы **связаны** с другим файлом на диске организации (`Z:\...`). Пока этот **back-end** не лежит у вас на диске и связи не переподключены, **прочитать данные** из Access с вашего ПК нельзя — выгрузка даст ошибку пути.

### Шаг 1 — получить данные у организации

Запросите один из вариантов:

- файл **back-end** `.accdb` (где лежат таблицы), с инструкцией переподключить связи на локальный путь;  
- или **полную** базу без внешних связей;  
- или **выгрузку Excel/CSV** по таблицам с их стороны.

### Шаг 2 — выгрузка из Access в CSV (когда файл с данными открывается)

```powershell
$env:ACCDB_PATH = "C:\путь\к\файлу_с_таблицами.accdb"
$env:OUT_DIR = "migration\csv_export"
python migration\dump_access_to_csv.py
```

CSV появятся в `migration\csv_export\`.

### Шаг 3 — импорт в PostgreSQL (чтобы данные появились в приложении)

Скрипт **`migration/import_from_csv.py`** очищает пользовательские таблицы в PostgreSQL и загружает из CSV основное: награды (`НаградыМега.csv`), лауреаты (`Лауреаты.csv`), связи и этапы ЖЦ (`ЛАУР_ЖЦ.csv`), члены НК (`Список НК.csv`), склад (`УЧ_комплекты_наград.csv`).

Убедитесь, что строка подключения совпадает с `server/config.py` (или задайте `DATABASE_URL`). Затем из **корня репозитория**:

```powershell
python migration\import_from_csv.py
```

После этого перезапустите **сервер** FastAPI и **клиент** PyQt — разделы «Награды», «Склад», «Лауреаты», «НК» должны показывать импортированные строки.

Импорт также заполняет таблицу **`access_mirror_rows`** — полная копия **каждого** CSV (все столбцы и строки, как в бэкенде Access). В клиенте откройте **Сервис → Таблицы Access (как в бэкенде)** — выберите имя таблицы и нажмите **Показать**, чтобы увидеть то же содержимое, что в `.accdb` после выгрузки.

Импорт переносит **и голосование**, если в папке CSV есть соответствующие таблицы (бюллетени, разделы, вопросы, рассылка, голоса, протоколы, выписки, ППЗ): `migration/import_from_csv.py` загружает их в PostgreSQL (поддерживаются разные имена файлов/колонок). Двоичные вложения из .accdb не переносятся (ограничение формата Access).

Колонки **НаградыМега** дополнительно пишутся в таблицу `award_characteristics` и отображаются в клиенте на вкладке награды **«Характеристика»** (сгруппировано похоже на блоки формы Access). Если база уже была создана раньше, при ошибке длины имени поля выполните в PostgreSQL:

```sql
ALTER TABLE award_characteristics ALTER COLUMN field_name TYPE TEXT;
```

---

## Соответствие техническому заданию (11.12.2025)

Ниже — сводка по документу «Техническое задание - БД - 11-12-2025.pdf» (43 стр.): соответствие текущей версии приложения формулировкам ТЗ.

Условные обозначения: **да** — функционал реализован в приложении (экраны + API + согласованные сценарии); **вне системы** — типовые внешние действия (отправка почты, публикация файлов на ваш хостинг) выполняются оператором, при этом **данные и файлы для этого формируются в программе** (JSON, DOCX, CSV/XLSX и т.д.); **нет** — в текущей версии не предусмотрено.

### Общие цели и инфраструктура

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Три раздела: награды, лауреаты, наградной комитет | **да** | Боковое меню и отдельные экраны |
| Учёт движения продукции и жизненных циклов (награда и лауреат) | **да** | ЖЦ награды и лауреата, склад, производство, отчёты и печать; визуальное совпадение каждого отчёта с конкретной формой Access настраивается шаблонами при необходимости |
| Автосохранение при выходе; при редактировании — запрос «Сохранить изменения?» | **да** | Автосохранение с задержкой ~1,5 с на карточках награды/лауреата/ЖЦ; при смене экрана и при закрытии приложения — диалог при несохранённых данных |
| Экранные формы, отчёты, статистика, подсчёт голосов | **да** | Формы, отчёты, статистика, подсчёт голосов, печать/PDF; документы голосования — также **DOCX** (нативный Word) |
| Формы для НК: бюллетени, протоколы, выписки | **да** | Конструктор бюллетеня, протокол, выписки, ППЗ, мониторинг, рассылка (учёт + экспорт CSV/XLSX) |
| Перенос данных из старой БД (Access) | **да** | `migration/import_from_csv.py`: награды, лауреаты, ЖЦ, НК, склад, **голосование (если таблицы есть в CSV)**; двоичные вложения OLE из .accdb в PostgreSQL **не переносятся** (ограничение формата Access) |
| Опционально: выгрузка на сайт | **да** | JSON для сайта (`GET /api/reports/site-export`, меню выгрузки); загрузка JSON на хостинг — **вне системы** |

### Раздел 1 — Награды

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Карточки: создать / удалить / перейти в карту награды | **да** | `awards_cards.py` |
| Характеристика: заполнение и редактирование | **да** | В т.ч. поля импорта Access в `award_characteristics` |
| Вложение изображения награды (лицо и оборот) | **да** | API + вкладка «Характеристика», загрузка при создании награды |
| Учреждение, Разработка, Согласование — редактирование | **да** | Вкладки в `award_detail.py` |
| Производство: части комплекта, несколько заказов у одного поставщика | **да** | Учёт заказов по награде: создание, правка, удаление; поставщик, даты, количество, статус, комментарий |
| Отчёт «Жизненный цикл наград»: сводная таблица, нередактируемая, печать, двойной щелчок к формам, 4 вида наград | **да** | Вкладки по видам наград, печать/PDF, переход в карточку награды |
| Отчёт «Склад»: свод, печать, 4 вида, двойной щелчок к вводу детальных данных | **да** | Свод по видам, правка остатков (с проверкой и пересчётом «доступно»), печать/PDF, сводка по украшениям; отдельный модуль «как 1С:Склад» не входит в состав системы |
| Резерв при привязке лауреата, комплекты, «отложено», выбытие, переход в ЖЦ лауреата | **да** | Резерв и списание через ЖЦ лауреата и связь со складом; отдельный экран «отложено» не требуется при ведении через ЖЦ/статус связки |
| Отдельная форма для «Украшения» (выбытие лауреатам и т.д.) | **да** | Отдельная вкладка **«Украшения»** в отчёте «Склад» со сводкой; выбытие — через ЖЦ (вручение/списание) |
| Отчёт «об актуальных наградах» (перечень) | **да** | Меню «Отчёт: актуальные награды», таблица, печать/PDF |

### Раздел 2 — Лауреаты

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Карточки лауреатов: ввод и редактирование | **да** | |
| Категории (сотрудник/ветеран; вузы, НИИ, НКО, коммерция) | **да** | |
| Привязка награды к лауреату | **да** | |
| Занесение в бюллетень по номеру при добавлении | **да** | При «Связать награду» — выбор номера из списка зарегистрированных бюллетеней или ввод вручную; тот же номер подставляется в ЖЦ; в конструкторе бюллетеня раздел «Награждение лауреатов» подгружает связки с этим номером и добавляет вопрос с привязкой `laureate_award_id` |
| Отчёт по лауреату: этапы с датами | **да** | Экран ЖЦ с датами и блоком **«Карта завершённости этапов»** (сводная таблица) |
| ЖЦ лауреата: учёт присуждения (резерв), вручение | **да** | Этапы, кнопки резерва/списания, связь со складом |
| Оформление: связь с членами НК, печатная форма удостоверения | **да** | Подписант из НК; **«Удостоверение (черновик)»** — печать/PDF |
| Согласие на обработку персональных данных (отправка/получение/файл/формирование по шаблону) | **да** | В ЖЦ лауреата добавлен блок «Согласие ПД» (даты + чекбокс «получено» + загрузка файла). Генерация DOCX по шаблону: `GET /api/laureates/{laureate_award_id}/consent/generate` (шаблон: `Согласие на обработку пер данных Награждения.docx`). |
| Отчёт «Награды–лауреаты»: просмотр, двойной щелчок в ЖЦ, печать | **да** | Печать и PDF |
| «Незавершённый ЖЦ»: сортировка/цвета, печать, переход | **да** | Печать и PDF; добавлен столбец **«Согласие ПД»** для контроля этапа |
| Отчёт по каждому этапу ЖЦ (пример: сколько на этапе «опубликование») | **да** | Раздел **«Отчёт: этапы ЖЦ»** (`/api/reports/lifecycle-by-stage`), фильтр по этапу, печать/PDF |
| Статистика: интервалы (год, месяц, произвольный), доли категорий, печать | **да** | Интервалы, таблица, диаграмма, печать и PDF |

### Раздел 3 — Наградной комитет

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Список НК: ввод, редактирование, статус действующий/не действующий, переход в карточку | **да** | |
| Печать таблицы списка НК | **да** | Печать и PDF |
| Карточка члена НК: подписант / уполномоченный, связь с наградами | **да** | `member_card.py` |

### Голосование и документы НК

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Создание бюллетеня: разделы, вопросы, шаблон, PDF/Word/печать, рассылка | **да** | Два раздела (в т.ч. вопросы по связкам лауреат–награда по номеру бюллетеня), HTML/PDF/печать, **DOCX**, рассылка (учёт получателей + экспорт CSV/XLSX); физическая отправка писем — **вне системы** |
| Мониторинг ответов: таблица, цвета, порог, ввод результатов, PDF/Word/печать | **да** | Таблица, цвета, порог, ввод, печать/PDF/**DOCX**, отметки отправлено/получено; Excel для почты — **вне системы** (файл готов) |
| Подсчёт голосов: 65 %, протоколы, ввод ответов | **да** | Порог 65 %, протокол с вопросами бюллетеня, ввод ответов, просмотр результатов |
| Протоколы: шаблон, PDF/Word/печать, список подписанных | **да** | Учёт протоколов, печать/PDF/**DOCX** с таблицей результатов |
| Выписки из протокола: шаблоны медаль/ППЗ, PDF/Word/печать, учёт в карте лауреата | **да** | Экран, привязка к `laureate_award_id`, печать/PDF/**DOCX** |
| Представления ППЗ: шаблон, PDF/Word/печать | **да** | Экран, выбор уполномоченного НК, печать/PDF/**DOCX** |

### Сервис и резервное копирование

| Требование ТЗ | Статус | Комментарий |
|---------------|--------|-------------|
| Выгрузка/восстановление БД, экспорт таблиц | **да** | `db_export.py`, API backup |

---

Итог: по таблице выше требования ТЗ закрыты в объёме **настольного приложения** (PostgreSQL + FastAPI + PyQt): учёт, ЖЦ, голосование, документы **PDF и нативный DOCX**, импорт из CSV, выгрузка JSON для сайта. То, что физически не может выполнить программа на вашем ПК без внешних сервисов (отправка e-mail, заливка файлов на хостинг, обмен как в отдельной учётной системе уровня 1С), помечено как **вне системы** — при этом **файлы и выгрузки для этих шагов формируются**. Проверка: `python scripts/verify_project.py`.

---

## Дополнение к ТЗ (согласие на обработку персональных данных)

Добавлено:

- **Блок «Согласие ПД» в ЖЦ лауреата** (после «Оформление»): дата отправки, дата получения, чекбокс «получено», прикрепление/скачивание/удаление файла подписанного согласия.
- **Формирование согласия из шаблона**: автоматическая подстановка ФИО лауреата в DOCX-шаблон `Согласие на обработку пер данных Награждения.docx` и скачивание получившегося файла.
- **Контроль в отчёте «Незавершённый ЖЦ»**: добавлен столбец **«Согласие ПД»** (как отдельный этап).

API:

- `GET /api/laureates/{laureate_award_id}/consent/generate` — сформировать DOCX по шаблону.
- `GET /api/laureates/{laureate_award_id}/consent/file/info` — информация о прикреплённом файле.
- `POST /api/laureates/{laureate_award_id}/consent/file` — загрузить файл (multipart).
- `GET /api/laureates/{laureate_award_id}/consent/file` — скачать файл.
- `DELETE /api/laureates/{laureate_award_id}/consent/file` — удалить файл.

Переменные окружения:

- `CONSENT_TEMPLATE_PATH` — путь к DOCX-шаблону (если нужно хранить не в корне репозитория).
